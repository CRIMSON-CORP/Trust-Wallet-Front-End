import React from "react";
import { HStack } from "native-base";
import IconWrapper from "./IconWrapper/inde";

const ActionButtons = () => {
    return (
        <HStack>
            <IconWrapper text={"Send"} />
        </HStack>
    );
};

export default ActionButtons;
